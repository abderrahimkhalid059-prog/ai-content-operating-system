services:
  postgres:
    ports:
      - '127.0.0.1:5432:5432'
  redis:
    ports:
      - '127.0.0.1:6379:6379'
  api:
    ports:
      - '${API_PORT:-3000}:3000'
  web:
    ports:
      - '${WEB_PORT:-5173}:8080'

networks:
  backend:
    internal: false
