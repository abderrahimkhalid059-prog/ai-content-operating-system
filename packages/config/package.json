{
  "name": "@ai-content-os/config",
  "version": "0.1.0",
  "private": true,
  "main": "dist/src/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@ai-content-os/shared": "*"
  }
}
