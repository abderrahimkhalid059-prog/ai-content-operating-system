{
  "name": "@ai-content-os/testing",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "ioredis": "^5.8.2",
    "supertest": "^7.1.4"
  },
  "devDependencies": {
    "@types/supertest": "^6.0.3"
  }
}
